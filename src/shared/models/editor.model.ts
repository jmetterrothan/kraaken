export enum EditorTerrainMode {
  ERASE = 0,
  PLACE = 1,
  FILL = 2,
  PICK = 3,
}

export enum EditorCollisionMode {
  ERASE = 0,
  PLACE = 1,
}

export enum EditorMode {
  TERRAIN = 0,
  ENTITY = 1,
  COLLISION = 2,
}
